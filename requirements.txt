# Python dependencies for the analysis/orchestration layer.
# The scanners themselves (gitleaks, semgrep, trivy, osv-scanner, zizmor) are
# installed as system tools / container images, NOT via pip.
#
# check_environment.py deliberately uses stdlib only so it runs before this
# file is installed.

# --- Data / analysis ---
pandas>=2.2,<3.0
numpy>=1.26,<3.0
scipy>=1.12,<2.0
statsmodels>=0.14,<1.0

# --- Figures ---
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0

# --- HTTP (non-invasive deployment availability checks only) ---
requests>=2.31,<3.0
urllib3>=2.0,<3.0

# --- Config / parsing / validation ---
PyYAML>=6.0,<7.0
python-dateutil>=2.9,<3.0
jsonschema>=4.21,<5.0

# --- Resilience / UX ---
tenacity>=8.2,<10.0        # exponential backoff for GitHub API calls
tqdm>=4.66,<5.0

# --- Reporting ---
jinja2>=3.1,<4.0           # HTML report templating
markdown>=3.6,<4.0

# --- Tests / quality ---
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
